import { PlaceholderDimentionDirective } from './placeholder-dimention.directive';

describe('PlaceholderDimentionDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaceholderDimentionDirective();
    expect(directive).toBeTruthy();
  });
});
